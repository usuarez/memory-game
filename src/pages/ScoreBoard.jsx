import React from 'react'
import Layout from '../components/Layout'

export default function ScoreBoard() {
    //this page would be available ASAP
    //save each finished game in firebase without auth.
    return (
        <Layout>
            <h1>ScoreBoard</h1>
        </Layout>
    )
}
